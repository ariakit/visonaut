# First trusted Ariakit visual run

The [Ariakit workflow](https://github.com/ariakit/ariakit/actions/runs/36054765510) completed its existing Linux Chrome/Firefox and macOS Safari `@visual` jobs, separate signed uploads, and submit job on September 24, 2026. Its CI Gate passed. [Visonaut run `fd59db1d-aa12-43d5-8f58-eaf9164713c8`](https://visonaut.com/runs/fd59db1d-aa12-43d5-8f58-eaf9164713c8) sealed after the GitHub completion webhook, reached a ready comparison, and reported a passing App check. This is a pull-request capture before a main-branch reference baseline. The new images were accepted automatically; it is not evidence of human baseline review or unchanged-image comparison against a main reference.

The service recorded **1,058 review variants** across 183 items, with 993 distinct original PNG objects and 58,024,028 original bytes. The existing Ariakit visual workflow runs 307 desktop tests. These are the actual grouped screenshot identities in this hosted path. The earlier [3,582-capture study](./ariakit-capture-measurement/README.md) split page sections into separate images, so its capture count and byte distribution must not be substituted for this run's hosted inventory. The issue's separate approximately 10,580-capture design workload and 35,820-capture stress sensitivity remain unproven by this pass.

The [sanitized before/after meter record](./e07-trusted-ariakit-run.json) covers September 24, 20:25:46–21:10:22 UTC. The three production Worker names added 1,823 service requests, 1,008 comparison requests, and 117 webhook requests, with zero sampled Worker errors. The production D1 database added 1,005,713 rows read and 45,962 rows written in the day-level analytics counter. The image bucket added 1,010 `PutObject` and 1,128 `GetObject` operations; quarantine added four puts and six gets. The production operations queue added 11 writes, 11 reads, and 11 deletes. All Cloudflare GraphQL calls returned HTTP 200 with no reported query errors. These are **project-level deltas around the run**, not exact per-run attribution: other traffic occurred, D1 is day-level, and provider analytics can lag or sample.

The measured D1 write delta is far below the 144.8 million synthetic capacity-fixture writes behind the [September 23 cost alert](./e07-cost-admission.md#september-23-cost-alert-attribution-and-cleanup). Do not repeat those one-off database stress probes. This first real workflow gives bounded service traffic and object counts, but it does not establish a final monthly invoice, run frequency, Worker CPU bill, backup churn, authoritative peak memory, or the configured tenfold target. Keep the numeric incremental monthly cost gate open until delayed billable usage and a defensible shared-account counterfactual are available.
