# Final-source E03 replay artifacts

The [hosted report](./results/final-hosted-report.json) preserves all seven state suites and 147 assertions run on September 23, 2026. The adapted fixture imports the exact production service, protocol, and comparator files from Git tree [`a953a2b`](https://github.com/ariakit/visonaut/commit/a953a2b42eb293088ba895484861756610de4f40). The [source manifest](./source-manifest.json) records all 42 imported source and migration file hashes. The [bundle manifest](./results/build-manifest.json) and [deployment record](./results/deployment.json) bind those sources and fixture files to the deployed Worker bytes and version.

Run `python3 verify.py` here to check every saved artifact hash, every archived source and build hash, all seven result records, D1 receipt readback, and R2 readback summary without network access. The [final harness archive](./final-harness.tar.gz) preserves the adapted scripts, 42 exact source files, migration files, and deployed bundle. It contains no credential. Its Worker exposes seven `POST /run/:scenario` suite routes, plus authenticated `GET /manifest` and `GET /receipt/:prefix` read routes. It has no GitHub sender route.

The fixture uses the isolated Worker and D1 database `visonaut-e03-final-20260923` and private R2 bucket `visonaut-e03-final-20260923-images`. The [deployment config](./deployment-config.json) has no production or preview binding, route, cron, or queue. Its only secret name is `PROBE_TOKEN`; the value is absent from the archive and committed results. The original frozen E03 resources remain unchanged. The new resources remain available for inspection and should be deleted after the evidence is accepted.

The [D1 readback](./results/final-receipt-readback.json) has no foreign-key errors and seven passing receipts with the same source tree and Worker version. The [R2 readback](./results/final-r2-readback.json) records exact SHA-256 and size checks for all 33 D1-referenced PNGs and two retained synthetic PNGs, plus a refused read of the GC-deleted key. It does not enumerate unrelated bucket objects.

For an isolated local replay on the pinned Node and Miniflare dependencies, extract `final-harness.tar.gz` into a fresh temporary directory, copy the root and `apps/web` package manifests from the repository, and link their installed `node_modules` directories. Run `node run-local.mjs` from `tooling/evidence/state-recovery-final` in that temporary directory. The [local runner](./final-harness.tar.gz) uses only local native D1/R2. The hosted controller has single-use prefixes and must not be rerun against an existing result.

The boundary remains synthetic trusted metadata, identities, clocks, and images. The status sender is recorded. This result verifies the final production source in native hosted storage; it does not exercise the production deployment, OAuth/OIDC, App webhook, or production `sendGitHubCheck` transport.
